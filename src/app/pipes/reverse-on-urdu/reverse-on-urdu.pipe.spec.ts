import { ReverseOnUrduPipe } from './reverse-on-urdu.pipe';

describe('ReverseOnUrduPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseOnUrduPipe();
    expect(pipe).toBeTruthy();
  });
});
