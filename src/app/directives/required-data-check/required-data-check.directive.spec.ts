import { RequiredDataCheckDirective } from './required-data-check.directive';

describe('RequiredDataCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredDataCheckDirective();
    expect(directive).toBeTruthy();
  });
});
