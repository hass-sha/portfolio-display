import { AttachModalDirective } from './attach-modal.directive';

describe('AttachModalDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachModalDirective();
    expect(directive).toBeTruthy();
  });
});
