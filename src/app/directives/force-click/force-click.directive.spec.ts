import { ForceClickDirective } from './force-click.directive';

describe('ForceClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ForceClickDirective();
    expect(directive).toBeTruthy();
  });
});
