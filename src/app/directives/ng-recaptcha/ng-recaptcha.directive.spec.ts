import { NgRecaptchaDirective } from './ng-recaptcha.directive';

describe('NgRecaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new NgRecaptchaDirective();
    expect(directive).toBeTruthy();
  });
});
